import * as React from 'react';
import { Container, Box } from '@mui/material';
import AddPackageData from '@/components/package-data/add-package-data';

export default function AddPackageData2() {
    return (
        <Container>
            <Box>
                <AddPackageData />
            </Box>
        </Container>
    );
}
